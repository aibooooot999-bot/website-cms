# 🏢 Website CMS - 企業官網內容管理系統

企業官網後台管理系統，提供頁面管理、使用者管理、權限控制等功能。

## ✨ 功能特色

- 🔐 **使用者認證** - JWT Token、安全登入
- 👥 **使用者管理** - 多帳號、角色分配
- 🛡️ **權限管理** - 角色權限、功能控制
- 📄 **頁面管理** - 新增/編輯/刪除頁面
- 🖼️ **媒體庫** - 圖片/檔案上傳管理
- ⚙️ **系統設定** - 網站基本資訊設定

## 🛠️ 技術棧

- **前端**: Vue 3 + Vite + TailwindCSS + Pinia
- **後端**: Node.js + Express + TypeScript
- **資料庫**: SQLite (better-sqlite3)
- **認證**: JWT + bcrypt

## 📁 專案結構

```
website-cms/
├── backend/
│   ├── src/
│   │   ├── index.ts          # Express 主程式
│   │   ├── database.ts       # SQLite 資料庫
│   │   ├── auth.ts           # JWT 認證中介層
│   │   └── routes/
│   │       ├── auth.ts       # 登入/登出
│   │       ├── users.ts      # 使用者管理
│   │       ├── roles.ts      # 角色權限
│   │       └── pages.ts      # 頁面管理
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── views/            # 頁面視圖
│   │   ├── components/       # UI 組件
│   │   ├── stores/           # Pinia 狀態
│   │   └── composables/      # 可複用邏輯
│   └── package.json
└── data/
    └── cms.db                # SQLite 資料庫
```

## 🚀 快速開始

### 安裝依賴

```bash
# 後端
cd backend
npm install

# 前端
cd ../frontend
npm install
```

### 啟動開發環境

```bash
# 終端機 1: 後端 (port 3001)
cd backend
npm run dev

# 終端機 2: 前端 (port 3000)
cd frontend
npm run dev
```

### 預設管理員帳號

- 帳號: `admin`
- 密碼: `admin123`

## 📋 角色權限

| 角色 | 說明 | 權限 |
|------|------|------|
| **超級管理員** | 最高權限 | 所有功能 |
| **管理員** | 一般管理 | 頁面/媒體管理 |
| **編輯者** | 內容編輯 | 頁面編輯 |
| **檢視者** | 僅檢視 | 唯讀存取 |

## 📄 License

MIT
